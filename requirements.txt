# Core Vercel Serverless Function Dependencies
# (Kept lightweight so Vercel does not exceed the AWS Lambda 250MB size limit)
# Note: For full local Streamlit & SQLite homework environment, install requirements-streamlit.txt
requests>=2.31.0
python-dotenv>=1.0.1
