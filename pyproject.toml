[project]
name = "aiot-l3-cwa-hw1"
version = "0.1.0"
description = "Taiwan Weather Map API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
]

[tool.vercel]
entrypoint = "api.weather:handler"
